In Progress/Done:
Purchase
- Allow to upload file.
- Add Pay Terms
- Freight Terms (FOB).
- Send pdf from mims.
- Cancel PO.
- Produced set scheduled and produced.
- Arrow on production.
Component:
Schedule:
- Show legend.
Customer:
Sale:
- Transfer to (show sales with 0 stock).
- Search by shipment #
Return:
- Validate that it cannot return more that shipped.
Receiving:
+ Allow to enter Packing list #.
Accounting:
- Bills view - incoming (recevings)
+ Show invoice #
+ Show price
+ Allow to search by Packing List # (show all components for that packing), 
+ Allow to enter $ amount for each component received
- Allow to enter invoice number for all
- Show Total Inventory value based on search (Component based on average cost)

- Invoice Item view - outgoing (our sales/shipments)
- Show total Inventory value (Item) stock value $ (based on material cost (components))
Invoices:
- Permissions
- Send email to client (andy as cc: by default).
- Total quantity
- Total price
Item:
- Show component picture when mouse over.
- Items ready for production and shippped.
- Items not ready for production and shipped.
Shipment:
- Select ship to address based on first sale (if not selected yet).
- Show DC in freight address list.
+ Shipment name. KASKA!!!
- Two and One week notification based on Window From (Routing)
Schedule:
- Email notification on create new delivery.
Global:
- Create document describing columns (Components, Items).
- Lock all PDFs. Option to lock.

------------------------------------------------------------------

Backlog
- Delete item from component not updating items because it doesn't exist in post sale.
- Keep the list of jms messages in requeuest until transaction is over. Then call JMS Receivier.
	Creating new shipment is not created until transaction completes. Jms runs before trans complete.
- Setting file
- Backup all schemas
- Backup files (compress?)
- Migration script run using DDL script for all tenants.
- Disable notifications when running unitsUpdate full.
- Disable notifications when creating new year context.
- add year to notifications.
- Price history/average (based on PO).
- Change notification to not use events.


